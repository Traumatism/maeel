"std" include

fun inline map (array} rot swap (over! rot rot+ swap) for drop)

fun inline reduce (swap ~ _reduce_fn swap (swap _reduce_fn!) for)

fun filter pr (array} ~xs (dup pr! ? (xs swap+ ~xs)) for xs)
