"std" include

fun inline map ({} rot swap (over! rot rot+ swap) for drop)

fun inline reduce (swap ~ _reduce_fn swap (swap _reduce_fn!) for)

fun filter pr ({} ~xs (dup pr! ? (xs swap+ ~xs)) for xs)
