"array" include
"core" include
"fp" include
"logic" include
"math" include
"string" include
"unix" include

fun inline panic (
    "PANIC: %" format putsln 1 0 %
)

fun inline assert_eq (
    rot = dup bool2string rot rot "% (%)" format putsln not ? ("" panic)
)
