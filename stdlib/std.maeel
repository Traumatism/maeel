λ println [] (print "\n" print drop)

λ assert_eq [
    value
    should_be
    _content
] (
    λ p [_content] (_content print drop)
    _content p ": " p should_be value =
    dup ⇒ ("OK\n" p)
    not ⇒ ("FAIL (" p should_be p ", " p value p ")\n" p)
)
