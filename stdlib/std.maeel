λ println [] (print "\n" print drop)

λ assert_eq [
    value
    should_be
    _content
] (
    _content print drop ": " print drop should_be value =
    dup ⇒ ("OK\n" print drop)
    not ⇒ ("FAIL (" print drop should_be print drop ", " print drop value print drop ")\n" print drop)
)
