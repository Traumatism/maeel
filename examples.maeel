include "maeel"

fun inline fact (
    dup 1=
	(drop 1)
	(dup 1- fact *)
    ifelse
)

fun map xs fn (
    List
	xs (fn!) for
    end
)

fun range a b (
    List
	a (dup 1+) (dup b<) while
    end
)


1 10 range (fact) map putsln
