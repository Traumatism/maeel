λ _pow (
    @ exp  ρ @ base ρ
    1 0 δ exp < ω (σ base * σ 1 + δ exp <)
    ρ
)

λ pow (
    @ exp  δ σ @ base ρ 0 < δ

    ⟹ (base exp! _pow 1 σ / σ)
    ! ⟹ (base exp _pow)
)

2 10 pow println
2 10! pow print