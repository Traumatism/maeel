proc main do
    dup 1 = ! while do
        dup 2 % 1 = if do
            3 * 1 + println
        end

        dup 2 % 0 = ? do
            2 / println
        end

        dup 1 = !
    end
end

15 main
