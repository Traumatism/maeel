"std" include

λ fib_impl [n a b] (
    n 0 = ⇒ (a)
    n 1 = ⇒ (b)
    n 1 > ⇒ (a b + b n 1 - fib_impl)
)

λ fib [n] (1 0 n fib_impl)

λ f [n] (
    n fib ⟶ Fn

    Fn 2 ∣ ⇒ (Fn)
    Fn 2 ∤ ⇒ (0)
)

{(f) ([1 34])} Σ println
