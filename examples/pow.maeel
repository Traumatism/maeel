proc pow do
    let pow_number pop
    let pow_exponent pop

    1  @ output
    0  @ ammount of iterations (0 <= pow_exponents)

    true while do
        swap pow_number * swap 1+ dup
        pow_exponent =!
    end

    pop
end

3 4 pow println
