proc sqrt do
    let sqrt_value 1

    0

    true while do
        swap dup
        sqrt_value /
        sqrt_value + 2 /

        let sqrt_value pop

        swap 1 + dup

        30 =!
    end

    pop pop

    sqrt_value
end
