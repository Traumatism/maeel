@ Calculate the n'th term of the
@ fibonacci sequence.
@
@ i.e. 10 fib_n println pop
proc fib_n
    dup dup 1= swap 0= or! if
        dup 1- fib_n swap 2- fib_n+
    end
end_proc

@ This shouldn't be called
@ outside of the fib_sequence
@ procedure.
proc fib_sequence_inner
    dup fib_n println pop
    1+ fib_sequence_inner
end_proc

@ Calculate all the terms of the
@ fibonacci sequence until stack
@ overflow error happen.
proc fib_sequence
    1 fib_sequence_inner
end_proc

fib_sequence
