proc fib_n
    dup dup 1= swap 0= or! if
        dup 1- fib_n swap 2- fib_n+
        return
    end
end_proc

proc fib_sequence_inner
    dup fib_n println pop
    1+ fib_sequence_inner
end_proc

proc fib_sequence 1 fib_sequence_inner end_proc

fib_sequence
