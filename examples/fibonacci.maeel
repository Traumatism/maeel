let a 0
let b 1

b 1_000_000 < while do  @ checks if b is <1,000,000
    b let c dup         @ c <- b
    a b + let b dup     @ b <- a + b
    println pop
    c let a pop         @ a <- c
    b 1_000_000 <       @ check if b is still <1,000,000
end
