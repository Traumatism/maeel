proc fib do
    dup dup 1 = swap 0 = | ! if do
        dup 1 - fib swap 2 - fib+
    end
end

proc main do
    let n 0
    let m pop

    n m = ! while do
        n 1 + let n pop
        n fib println pop
        n m = !
    end

    del n
    del m
end

15 main
