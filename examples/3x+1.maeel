λ main (
    δ 1 = ! ω (
        δ 2 % 1 = ? (
            3 * 1 + δ print ρ " -> " print ρ
        )

        δ 2 % 0 = ? (
            2 / δ print ρ " -> " print ρ
        )

        δ 1 = !
    )
)

9999999 main
