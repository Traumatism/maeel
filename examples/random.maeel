include "maths"

15 fact   → seed
4097      → a
11 fact   → c
429496729 → m

λ calculate [] (seed a * c + m % δ → seed)

λ generate_random_numbers [] (
    {(calculate 15 %) ([1 15])}
)

generate_random_numbers print
