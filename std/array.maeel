proc contains do
    let array  pop
    let value  pop
    let result false

    array for do
        value = result true =! * if do
            true let result pop
        end
    end

    result
end


proc any do
    true swap contains
end


proc sum do
    0 swap for do + end
end


proc product do
    1 swap for do * end
end